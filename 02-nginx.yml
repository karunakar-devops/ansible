- name: Install and run nginx
  hosts: web
  become: yes
  modules:
  - name: Installing Package
    ansible.builtin.dnf:
      name: nginx
      state: latest

  - name: Starting service
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes
